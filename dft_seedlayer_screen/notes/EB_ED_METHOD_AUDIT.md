# E_b는 얼마나 믿을 수 있고, E_d는 어떻게 계산해야 하는가

작성: 2026-09. 이 세션에서 발견한 오류·정정·한계를 한 곳에 모은 것.
목적: 논문에 어떤 숫자를 어떤 단서와 함께 실을지 결정하기 위함.

---

# 1. E_b — 지금 데이터의 신뢰도

## 1.1 실제 프로토콜 (scripts/05, 06)

```
방법   PBE-D3BJ / def2-SVP (Ag: def2-ECP), UKS 이중항, counterpoise 보정
기하   분자는 GFN2-xTB 최적화 구조로 고정(frozen). Ag만 움직임
탐색   결합축 1차원 강체 스캔, 최소점 근처 0.1 Å 간격 7점
       일부는 Ag 위치만 DFT로 재최적화(05) 후 측면 오프셋 스캔
```

**즉 "완전 최적화된 착물의 결합에너지"가 아닙니다.** 분자가 Ag를 만나 완화될 여지를
주지 않았으므로 **참값의 하한**입니다. 강한 결합자(HATCN, F4TCNQ)일수록 완화 이득이
클 테니, **실제 서열 격차는 표보다 더 벌어질 가능성이 높습니다.**

## 1.2 신뢰도 등급

### A등급 — 그대로 인용 가능
`HATCN(CN) 1.029` · `F4TCNQ 0.966` · `Cs2CO3 0.900` · `Bphen 0.490` ·
`Mo3O8 0.445` · `Al4O6 0.422` · `pyridine 0.342` · `triazine 0.291` ·
`BTD 0.277` · `Me3P=O 0.253` · `LiF32 0.249` · `thiophene 0.169` ·
`benzene 0.167` · 프래그먼트 10종

스캔 최소점이 격자 위에 있고(2.338, 2.541, 2.896처럼 비정수), 앵커가 확인됨.

### B등급 — 값은 쓰되 배위 모드는 미확정
`TPBi 0.889` · `p-bPPhenB 0.870`

`psi4_binding_refined_eV.json`의 `_offset` = 0.0 — 측면 오프셋 탐색에서
개선이 없었다는 뜻. 저장된 구조는 Ag가 **정확히 2.30 Å**에 있어, 이 파일만으로는
단좌/이좌를 판정할 수 없음. 이좌라는 감사 결과는 별도 전체최적화에서 나온 것.

### C등급 — 구조 자체가 비물리적, 재계산 필요
`p-bPPhenB 구조` (Ag–N **1.39 Å**) · `B3PyMPM 0.626` (Ag–H **1.85 Å**) ·
`Liq 0.167` (Ag–Li **0.91 Å**)

전부 결합거리가 아님. **초기 배치가 최적화 없이 그대로 남은 것.**
`pathgeom.sanity()`가 이제 이 구조들을 거부함(scripts/97, 102).
E_b 값 자체는 별도 스캔에서 나왔을 수 있으나 **구조와 값의 대응이 확인 안 됨.**

### 기각 — 표에 넣으면 안 됨
`Al2O3 0.910` (Al-rich Al10O10 과결합, 정량비는 0.422) ·
`MoO3 -0.718` (미정련, 정련값 +0.284) · `ZnS 1.60` (문헌 추정, 미계산) ·
`PhCN·ZnS 1.218` (ZnS–나이트릴, Ag 무관) · `HATCN face -0.207` (미정련, -0.024) ·
xTB 값 전부 (HATCN 3.08, TPBi 2.14 — DFT를 덮어쓴 전력 있음)

## 1.3 방법론적 한계 (A등급에도 해당)

| 한계 | 영향 | 서열에 영향? |
|---|---|---|
| 분자 고정(rigid) | 참값의 하한 | 거의 없음 (모두 동일 처리) |
| def2-SVP (이중제타) | CP 보정해도 ±0.1 eV 수준 | 작음 |
| PBE (GGA) | 전하이동계에서 비편재화 오차 | **강한 억셉터에서 과대평가 가능** |
| 이중항 가정 | Ag⁺/분자⁻ 전하이동 상태 미검토 | **미확인** |
| Ag 단원자 | 실제 임계핵은 Ag_n일 수 있음 | Venables 전제와는 일치 |
| 무기물은 소형 클러스터 | 표면이 아님 | **Al2O3 사례가 실증** |
| 단좌 vs 이좌 혼재 | 자리당 값이 아님 | **큼 — 반드시 분리** |

**가장 큰 위험은 GGA의 비편재화 오차입니다.** HATCN·F4TCNQ 같은 강한 억셉터에서
PBE가 전하이동을 과도하게 안정화시켜 E_b를 부풀렸을 수 있습니다.
**검증법: 상위 3~4종만 ωB97X-D 또는 M06-2X/def2-TZVP로 재계산.**
서열이 유지되면 전체 스크리닝이 방어됩니다.

## 1.4 논문에 쓸 때

- **절대값을 주장하지 말고 서열을 주장할 것.** 동일 프로토콜 내 상대비교는 견고함
- 단좌/이좌를 **같은 축에 세우지 말 것**
- E_b는 **자리당** 값이며, 핵생성을 정하는 것은 **자리밀도 × 결합**
  (scripts/111: HATCN 13.7 /nm², TPBi 2.1 /nm²)
- C등급 3종은 재계산 전까지 표에서 제외

---

# 2. E_d — 무엇을 어떻게 계산해야 하는가

## 2.1 현재 상태

| | 값 | 출처 |
|---|---|---|
| HATCN | 0.286 eV | scripts/24, DFT |
| Al4O6 (AlOx) | 0.170 eV | scripts/24, DFT |
| clean Al | 0.000 | 참조 |
| TAPC | −0.007 | 참조 (사실상 0) |

**나머지 16종은 미계산.** GFN2-xTB 스크린은 **기각** — DFT 값이 있는 두 계에서
모두 0.000 eV를 반환(0.286, 0.170 대비). scripts/101에 기록.

## 2.2 이 세션에서 잡은 프로토콜 버그 두 개

**① 경로가 분자를 관통했습니다.** 두 결합자리 사이를 직선 보간하면 중점에서 Ag가
기판 원자와 결합거리까지 접근 → 최대 **964 eV** 같은 값이 나왔습니다.
이제 매 스텝 표면 법선으로 밀어내 최근접 2.2 Å를 유지합니다(`pathgeom.place`).

**② 출발점만 높이 스캔에서 제외했습니다.** "이미 최적화된 구조"라는 논리였는데
DFT 기준으로만 최적입니다. 다른 방법에서는 출발점이 경로 최고점이 되어
**E_d가 정확히 0**으로 나옵니다. 이제 모든 점을 동일 처리합니다.

두 버그 모두 **원래 DFT 캠페인에도 있었습니다.** xTB 우회 시도가 이를 드러냈습니다.

## 2.3 현재 프로토콜의 근본적 한계

```
E_d = max(경로) − E(출발점)
경로 = 최근접 등가자리로 직선 드래그, 기판 고정, Ag 높이만 스캔
격자 = 경로점 3~5, 높이 2점  (4코어 제약으로 축소)
```

**이건 안장점 탐색이 아니라 성긴 강체 스캔입니다.** 얻는 값은 **하한** —
성긴 경로가 진짜 안장점을 놓칠 수는 있어도 없는 장벽을 만들지는 않습니다.

더 근본적으로 **고립 분자 하나 위의 확산장벽은 실제 막의 확산장벽이 아닙니다.**

- 자리가 **분자당 여러 개**인 경우(HATCN 6, F4TCNQ 4)만 분자내 hop이 물리적
- 자리가 **하나**면(pyridine, PhCN) 이동은 분자간이고, 그건 **패킹**이 정합니다
- 그래서 `site2site` / `toface` / `chelate` 세 class를 값과 함께 기록합니다.
  **class가 다른 값을 한 줄에 세우면 안 됩니다** — 단좌/이좌와 같은 문제

## 2.4 계산 수준의 사다리

| 등급 | 방법 | 비용 | 얻는 것 |
|---|---|---|---|
| **T1 (현재)** | 고립분자, 강체 드래그, 3~5점 | 시스템당 ~1 h (64코어) | class 내 **서열**. 절대값 신뢰 불가 |
| **T2** | 경로점 9~11 + 각 점에서 Ag 완전완화 | 5~10배 | 안장점 위치 개선. 여전히 고립분자 |
| **T3** | **NEB**, 분자 2~3개 클러스터로 막 표면 모사 | 30~50배 | 분자간 hop 포함. 실제 막에 근접 |
| **T4** | 주기적 슬랩 + NEB (VASP/CP2K) | 100배+ | 참값. `env/`에 cp2k 있음 |

**권장: T1으로 전 후보 서열을 잡고, 상위 3~4종만 T3로 검증.**
논문에는 T1을 "screening-level barrier, lower bound"로 명시하고
T3 결과를 상위 후보의 확증으로 씁니다.

## 2.5 실행 경로

- 스크립트 완성됨: `97_diffusion_barriers.py` (psi4), `102_ed_gaussian.py` (G16)
- 경로 생성은 `pathgeom.py`로 공유 — 두 드라이버가 갈라질 수 없음
- **4코어 컨테이너에서는 불가** (31원자 SCF 하나에 ~10분). 워크스테이션 64코어 필요
- Gaussian `SCF=(XQC)`가 psi4의 수렴 문제를 해결. pyridine에서 psi4와 **6 meV 일치** 확인
- 출력 분리(`GAUSS_OUT`) + 병합(`103_merge_Ed.py`)으로 4창 병렬 가능
- C등급 구조 3종은 자동 거부됨 → **재최적화 후 재투입 필요**

## 2.6 E_d 없이도 할 수 있는 것

E_d가 전 후보에 없어도 **자리밀도 × E_b**만으로 closure thickness 예측이 나옵니다
(scripts/111). E_d는 그 예측을 정교하게 만들 뿐 뒤집지 않습니다 —
Venables에서 E_d는 exp(χ·E_d/kT), χ=0.284이므로 **0.1 eV 차이가 3배**입니다.
따라서 **후보 간 E_d 차이가 0.1 eV를 넘으면 서열이 바뀔 수 있고,
그것이 E_d를 계산해야 하는 유일한 이유입니다.**

---

# 3. 우선순위

1. **C등급 3종 구조 재최적화** — 반나절. 안 하면 표에 못 씀
2. **E_d 전 후보 (T1)** — 워크스테이션 하룻밤
3. **상위 4종 E_b를 하이브리드 범함수로 재계산** — GGA 비편재화 오차 방어
4. **상위 3종 E_d를 T3 (NEB)로** — 논문의 메커니즘 절
5. 실험 검증: 시드 6종 closure thickness — **계산보다 이게 더 결정적**

---

# 부록: Gaussian 실행에서 확인된 것 (2026-09-10)

## 밀도적합은 쓰면 안 됨

벤젠 동일 구조, 동일 route에 `/Auto`만 추가한 대조:

| route | E (Hartree) | 사이클 | 시간 |
|---|---|---|---|
| 원본 `SCF=(XQC,MaxCycle=128)` | −378.729900502 | — | ~29 s (여유 머신) |
| `+VShift=300, MaxCycle=64` | −378.729900502 | 40 | 410 s (공유 머신) |
| `+/Auto` (밀도적합) | **−378.738217930** | 23 | 833 s |

- **VShift는 답을 바꾸지 않는다** — 원본과 소수점 9자리까지 동일. 수렴 경로만 바꾼다는 것이 실증됨
- **밀도적합은 답을 0.226 eV 바꾸고 2배 느리다.** 자동 생성 fitting set의 오차이며, 두 가지 이유 모두로 기각

psi4는 `scf_type df`를 썼으므로 두 프로그램 값의 직접 비교에는 이 차이가 잠재적 오차원이다.
다만 psi4의 DF는 최적화된 표준 auxiliary basis를 쓰고 Gaussian `/Auto`는 자동 생성이므로
동급이 아니다. **프로그램 간 비교는 HATCN 한 점으로만 하고, 나머지는 프로그램 내부 서열로 쓴다.**

## SCF 수렴 실태

Bphen (468 기저함수, Ag + 페난트롤린):

```
DIIS 129 사이클 → 수렴 실패, E = -1178.65242274
Quadratic Convergence SCF Method 전환
QC 7 사이클 → 수렴, E = -1178.68912812   (DIIS 값보다 1.0 eV 낮음)
S**2 = 0.7522 (이중항 이론값 0.75)
총 34분
```

- DIIS는 수렴 근처에도 못 갔고 ΔE 부호가 매 사이클 뒤집히는 전형적 진동
- **QC 자체는 7 사이클로 빠르다.** 비용은 앞의 129 사이클이 전부 버려지는 것
- 작은 분자(≤240 기저함수)는 DIIS가 정상 수렴 — 큰 분자에서만 발생

## 병렬 관련 (정정)

`Job cpu time ≈ Elapsed time`을 보고 단일 코어 실행을 의심했으나 **오진**.
작업관리자 CPU 100% 확인됨. G16W는 CPU 시간을 마스터 스레드 기준으로 찍는다.

**공유 머신에서는 `%NProcShared`를 실제 확보 가능한 코어 수로 낮추는 편이 빠르다.**
다른 사용자와 32코어를 다투면 스레드 전환 비용만 늘어난다.

## SCF 해의 불안정성 — 이 프로젝트의 가장 큰 방법론적 문제 (2026-09-10)

Bphen(468 기저함수)과 HATCN(444)에서 **DIIS가 수렴하지 않고 헤맵니다.**

동일 구조 Bphen을 세 번 돌린 DIIS 종점:
```
-1178.6525 (129 사이클)  /  -1178.7037 (65)  /  -1178.6325 (129)
폭 1.94 eV
```

HATCN은 사이클마다 ΔE가 **±0.36 eV**로 진동합니다. 128 사이클 후에도 진동 중.

세 경우 모두 Gaussian이 `Quadratic Convergence`로 전환한 뒤에야 수렴했고,
QC 결과는 DIIS 종점보다 **0.5~0.6 eV 낮습니다.**

### 결과적으로

1. **DIIS 계열 수렴(레벨시프트·댐핑 포함)은 이 계에서 신뢰할 수 없습니다.**
   psi4의 `ROB` 설정(level_shift 1.0 + damping 15%)도 DIIS 계열입니다.

2. **따라서 `runs/diffusion_barrier_eV.json`의 HATCN 0.286과 AlOx 0.170도
   검증 기준으로 쓸 수 없습니다.** 같은 약점을 가진 두 번째 측정일 뿐입니다.
   이 세션에서 psi4를 재실행했을 때도 −1487.24 근처에서 동일하게 진동했습니다.

3. **E_b 값들도 재검토 대상입니다.** 같은 프로토콜(psi4 + DIIS 계열)로 계산됐고,
   HATCN·F4TCNQ 같은 강한 억셉터가 정확히 이 문제가 생기는 계입니다.
   0.6 eV 규모의 해 차이는 E_b 서열을 바꿀 수 있습니다.

### 대응

- 확산장벽: `SCF=(QC,MaxCycle=64)` — DIIS 단계를 아예 건너뜀 (v4)
- 경로 전체를 `%Chk` + `Guess=Read`로 연결 — 모든 점을 같은 전자상태에 고정
- **E_b 상위 후보 재계산 시에도 QC를 써야 함.** 하이브리드 범함수 재계산
  (§1.3에서 권고한 것)을 할 때 함께 처리하면 한 번에 끝남

### 아직 모르는 것

QC가 찾는 해가 진짜 바닥상태인지는 `Stable=Opt`로 확인해야 알 수 있습니다.
변분적으로 더 낮으므로 DIIS 해보다는 낫지만, 그보다 더 낮은 해가 있을 가능성은
배제되지 않았습니다. 상위 3~4종에 대해서만이라도 안정성 검사를 권합니다.

## 수렴 기준 완화 (v5, 2026-09-10)

QC 전환으로 진동은 잡혔으나 **수렴 선언이 안 됩니다.**

HATCN_t0_z0, `SCF=(QC,MaxCycle=64)`:
```
반복 13  E = -1487.254882549   ΔE = -5.9 ueV   Grad = 5.369e-3
반복 18  E = -1487.254883215   ΔE = -2.9 ueV   Grad = 5.659e-3  ← 기울기가 상승
RMSU = 9.98e-08
```

에너지는 소수점 7자리까지 평평한데 **기울기가 오히려 올라갑니다.**
알파 HOMO-LUMO 간격이 0.008 Hartree(0.22 eV)라 궤도 공간이 거의 축퇴돼 있고,
**에너지를 바꾸지 않는 회전 방향**이 존재합니다. Gaussian은 그 기울기를 0으로
만들려 하지만 원리적으로 불가능합니다. 64 반복을 다 쓰고 실패로 기록됩니다.

### 판단 근거

- 로그의 RMSU가 이미 1e-7. Gaussian 기본값은 1e-8 요구
- 장벽 0.29 eV = 0.0107 Hartree. 1% 분해능에 필요한 것은 1e-4 Hartree
- 에너지는 1e-7 Hartree로 안정 → **세 자릿수 여유**

→ `Conver=6` (밀도 1e-6)로 완화. 이미 만족하는 수준이며 목적에 과분한 정밀도.

### 최종 설정

```
#P PBEPBE/Def2SVP EmpiricalDispersion=GD3BJ SP SCF=(QC,MaxCycle=64,Conver=6) NoSymm
%Chk=<folder>.chk   +   Guess=Read  (첫 작업 제외)
```

### 설정 변경 이력과 근거

| 버전 | 변경 | 근거 |
|---|---|---|
| v1 | `XQC,MaxCycle=128` | 초기값 |
| v2 | `+VShift=300, MaxCycle=64` | Bphen DIIS 128 사이클 낭비 |
| v3 | `+%Chk/Guess=Read` | **같은 구조가 0.51 eV 다른 해로 수렴** |
| v4 | `QC` (DIIS 제거) | DIIS 종점이 세 실행에서 1.94 eV 폭으로 산포 |
| v5 | `+Conver=6` | 축퇴로 기울기 수렴 불가, 에너지는 이미 1e-7 |

v1~v2 결과(Al4O6, benzene)는 궤도 연결이 없어 폐기.

---

## 진단 정정: 수렴 기준이 아니라 파동함수 불안정성 (v6, 2026-09-11)

v5(`SCF=(QC,MaxCycle=64,Conver=6)`)로 HATCN_t0_z0를 밤새 돌린 결과,
완화된 기준으로도 수렴하지 않았다. 로그가 원인을 직접 말해준다.

```
 Angle between quadratic step and gradient=  90.01 degrees.
 Incorrect curvature in search direction -- initial direction reversed.
```

이 두 줄이 33회 / 17회 반복된다. **정확히 90도**는 Newton 스텝이 기울기와
직교한다는 뜻이고, 이는 남은 기울기가 **음의 곡률 방향**에 놓여 있다는 것이다.
즉 궤도회전 공간의 **안장점** — 불안정한 파동함수다.

| iter | EE | Delta-E | Grad | Max rot |
|---|---|---|---|---|
| 12 | −1487.25488233 | −4.7e-5 | 5.460e-03 | 0.1704° |
| 13 | −1487.25488255 | −2.2e-7 | 5.369e-03 | 0.1682° |
| 43 | −1487.25489163 | −2.5e-9 | 5.962e-03 | 0.1846° |

에너지는 1e-9 Hartree까지 평평한데 기울기는 5.96e-03에서 **움직이지 않고**
회전각도 0.1847°에 고정된 극한주기(limit cycle)에 갇혀 있다. 어떤 스텝
길이로도 그 방향의 에너지를 낮출 수 없으므로, **수렴 기준을 아무리 완화해도
QC는 끝나지 않는다.** v5의 `Conver=6`은 잘못된 진단이었다 — QC의 종료 판정은
밀도가 아니라 기울기에 걸려 있다.

### 근본 원인

alpha HOMO–LUMO gap = 0.008 Hartree = **0.22 eV**.
PBE(GGA)의 비편재화 오차가 Ag 5s 전자를 억셉터 쪽으로 번지게 해서 전하이동
정도가 결정되지 않고, 프런티어 궤도가 준축퇴 상태가 된다. 이 준축퇴 공간의
궤도회전은 에너지를 거의 바꾸지 않으므로 안장점이 생긴다.

같은 오차가 **두 SCF 해가 0.08 eV 떨어져 있는 원인**이기도 하다.

| 경로 | E (Hartree) | 차이 |
|---|---|---|
| v4 DIIS-family QC | −1487.257852 | — |
| v5 순수 QC (안장점) | −1487.254892 | +0.0030 Ha = **0.081 eV** |

측정 대상 장벽이 0.29 eV이므로 해 선택의 모호성만으로 **28% 오차**다.
수치 문제가 아니라 물리 문제: Ag⁰/HATCN⁰ 대 부분전하이동 상태의 구분이
GGA 수준에서 서지 않는다.

### v6 처방: 범함수를 수렴 도구로 사용

```
#P PBE1PBE/Def2SVP EmpiricalDispersion=GD3BJ SP SCF=(XQC,MaxCycle=128,VShift=300) NoSymm
%Chk=<folder>.chk   +   Guess=Read  (첫 작업 제외)
```

- **PBE0 (PBE1PBE)** — 정확교환 25%가 gap을 벌려 준축퇴를 없애므로 안장점이
  사라지고, 동시에 두 해를 갈라놓던 비편재화 오차 자체가 줄어든다. 정확도
  개선이 아니라 **수렴 대책**으로 도입한다(정확도는 부수효과).
- **VShift=300** (0.3 Ha) — DIIS 단계에서 프런티어 궤도를 인위적으로 분리.
  고정점에서 레벨시프트는 virtual 고유값만 이동시키므로 **수렴한 밀도는
  진짜 SCF 해**다. 도달하는 해를 바꾸지, 그 해가 무엇인지를 바꾸지 않는다.
- **XQC** — QC를 1차 수단이 아니라 대비책으로. 여기서 QC 한 iteration은
  LinEq 미세반복을 ~60회 돈다. 정확교환 비용보다 훨씬 비싸다.

정지된 QC 43 iteration을 DIIS ~20 iteration으로 바꾸는 것이므로 hybrid임에도
**벽시계 시간은 줄어들 것으로 예상**한다.

### 소급 효과

E_b 값들은 PBE + DIIS 계열로 얻어졌다. 위 두 해의 0.08 eV 간격은
**E_b 표에도 같은 크기의 불확정성이 있다는 뜻**이다. 상위 3~4개 후보는
PBE0로 재계산해야 순위를 주장할 수 있다 (EB_SCREEN_TABLE.md의 등급 A도
이 재계산 전까지는 잠정).

### 설정 변경 이력 (갱신)

| 버전 | 변경 | 근거 |
|---|---|---|
| v1 | `XQC,MaxCycle=128` | 초기값 |
| v2 | `+VShift=300, MaxCycle=64` | Bphen DIIS 128 사이클 낭비 |
| v3 | `+%Chk/Guess=Read` | 같은 구조가 0.51 eV 다른 해로 수렴 |
| v4 | `QC` (DIIS 제거) | DIIS 종점이 세 실행에서 1.94 eV 산포 |
| v5 | `+Conver=6` | 축퇴로 기울기 수렴 불가 — **오진, 효과 없음** |
| v6 | `PBE0 + XQC,VShift=300` | 90° 안장점 = 파동함수 불안정, gap 0.22 eV |

---

## v5 완주 54개 작업 점검 (2026-09-11)

HATCN를 제외하고 7개 폴더 54개 작업이 돌아왔다. **54/54 Normal termination**이며
v6에서 진단한 90° 안장점은 HATCN 외에는 나타나지 않았다. 그러나 **에너지는
그대로 쓸 수 없다.** SCF 설정과 무관한 결함 세 가지가 따로 있다.

### 결함 1 — 각 폴더의 첫 작업이 다른 SCF 해로 수렴

`t0_z0`는 폴더에서 유일하게 `Guess=Read`가 없는 작업(Harris guess)이다.
7개 중 3개가 붕괴했다.

| 작업 | 폴더 최저값 대비 | S² |
|---|---|---|
| benzene_t0_z0 | **+3.408 eV** | 0.7567 |
| DMABN_t0_z0 | **+2.204 eV** | 0.7534 |
| Bphen_t0_z0 | +0.154 eV | 0.7551 |
| (정상 점들) | — | 0.7512–0.7519 |

체인이 그 궤도를 읽고 빠져나오느라 고생한 흔적도 남아 있다:
`DMABN_t0_z1` **39 iteration**, `benzene_t0_z1` **25 iteration** — 하류의
5~10 iteration과 대조된다. `Bphen_t0_z1`은 오히려 0.61 eV **올라갔고**
17 iteration, 이어서 `t1_z0`이 27 iteration으로 되돌아왔다.

**t0은 장벽의 기준점**이므로 이 한 점이 폴더 전체의 에너지차를 오염시킨다.

**S²가 자동 판별자로 쓸 만하다.** 이중항의 정확값은 0.75이고, 잘못된 해에
앉은 점은 예외 없이 0.7525 이상, 이웃과 일치하는 점은 0.7519 이하였다.
→ `123_read_gjf_logs.py`에 `S2_MAX = 0.7525` 자동 기각 추가.
반환된 v5 데이터에 적용했더니 정확히 그 세 점만 걸러냈다.

**수정**: 각 폴더 끝에 `<name>_t0_z0_ref.gjf`를 추가. 같은 구조를 체인이
끝난 궤도에서 다시 푼다. 싼 작업 하나로 기준점을 경로와 같은 전자 상태에
올려놓는다.

### 결함 2 — Cu4I4의 "1.5 eV 장벽"은 확산 장벽이 아니라 Pauli 반발

| 점 | Ag–I 거리 | E 상대값 |
|---|---|---|
| t0_z0 | 3.10 Å | 0.007 eV |
| t1_z0 | 2.32 Å | 1.033 eV |
| t2_z0 | 2.24 Å | **1.490 eV** |
| t3_z0 | 2.25 Å | 1.313 eV |
| t4_z0 | 2.45 Å | 0.000 eV |

경로 중간에서 Ag가 요오드 쪽으로 **0.8 Å 밀려 들어갔다**. 원인은 `place()`가
모든 원소쌍에 2.2 Å 하나만 쓴 것 — Ag–I 공유결합 거리는 2.84 Å다. 0.5 Å 위의
z1 계열이 0.29 eV밖에 안 되는 것이 결정적 증거다. 실제 화학흡착 장벽이
0.5 Å 만에 5배로 무너지지 않는다.

**수정**: 접촉거리를 공유반경(Cordero 2008) 합으로 원소쌍마다 계산.
Ag–I 2.84 Å, Ag–C 2.21 Å, Ag–Cs 3.89 Å. Cu4I4 경로가 2.24–3.10 Å에서
**2.85–3.10 Å**로 평탄해졌다. **모든 후보의 기하가 바뀌므로 v5/v6 출력과
섞을 수 없다.**

### 결함 3 — 경로 3점으로는 장벽을 볼 수 없다

t = 0, 0.5, 1에는 내부점이 없다. 보고되는 최대값은 더 높은 쪽 끝점일 뿐이다.

| 후보 | z0 경로 (eV) | 상태 |
|---|---|---|
| Cs2CO3 | 0.023, 0.000, 0.135 | 끝점에서 **아직 상승 중** |
| Bphen | 0.003, 0.093, 0.103 | 끝점에서 아직 상승 중 |

즉 하한값이지 장벽이 아니다. **수정**: `NPATH_MIN` 3 → 5 (긴 hop은 7).

또 Al4O6는 두 높이 모두 경로 전체가 단조 하강(0.230 → 0.047 eV)이었다.
원인은 결함 4(아래)로 밝혀졌다 — 경로가 거꾸로였던 것이 아니라, **애초에
hop이 없었다**.

### 그래도 살아남은 물리

t0_z0을 버리고 보면 DMABN(0.027 eV 폭), benzene(0.008 eV 폭), BTD(0.015 eV)는
**평탄하다**. 평범한 방향족 위에서 Ag 흡착원자는 사실상 자유확산한다
(<0.1 eV). 예상과 맞고, **HATCN 값이 의미를 갖는 것은 바로 이 대조 때문**이다.

### 작업량

54개가 약 3시간(02:56–06:02). v7은 246개이므로 12~15시간 + 난제 추가분.


### 결함 4 — 경로가 출발 원자를 떠나지 않았다 (Al4O6 단조 하강의 진짜 원인)

`destination()`이 목적지 원자 **위**의 위치를 계산할 때 **출발 원자의 법선**을
그대로 재사용했다. 3차원 분자에서 그 방향은 분자 **안쪽**을 향하므로, `place()`가
다시 밖으로 밀어내면 출발점 근처로 되돌아온다. v5 Al4O6의 매 점 최근접 원자:

| 점 | 최근접 | 목적지 O6까지 |
|---|---|---|
| t0 | O4 : 2.20 Å | — |
| t1 | O4 : 2.23 Å | — |
| t2 | O4 : 2.21 Å | — |
| t3 | O4 : 2.24 Å | **3.00 Å** |

**경로에 hop이 아예 없었다.** 0.230 → 0.047 eV는 장벽이 아니라 Ag가 산소 하나
주위를 미끄러져 내려간 것이다. 이 결함은 Al4O6에만 국한되지 않고 평면이 아닌
모든 후보(Cu4I4, Mo3O9, Cs2CO3, LiF32)에 같은 방식으로 작용한다.

**수정 두 가지**

1. `outward()` — 각 site마다 **자기 법선**을 계산한다. 구면(Fibonacci) 위
   1200 방향 중 모든 기판 원자로부터 (원소별 접촉거리 기준) 여유가 최대인
   방향을 고른다. 대칭 정보 없이 cage·평면 어디에나 쓸 수 있다.
2. `frames()` — 경로를 **site와 법선을 따로 보간**한다. 흡착원자의 절대위치를
   직선 보간하면 분자를 관통하는 현(chord)이 되고, 그걸 하나의 고정 법선으로
   밀어내는 것이 바로 위 붕괴를 낳았다. site를 표면 위로 걸어가게 하고 법선을
   같이 회전시키면 흡착원자가 내내 표면 위에 머문다.

v7 Al4O6: `O4:2.20 → O4:2.23 → Al0:2.72 → O6:2.53 → O6:2.20` — 진짜 hop이다.

또한 site2site 경로는 끝나면 `got == anchor`인지 검사해 **출발 원자로 되돌아온
경로를 기각**한다.

#### 부수 효과: 공짜 오차막대

site2site 경로가 이제 동등한 두 원자 사이를 **같은 높이로** 오간다.

| 후보 | 시작 | 끝 |
|---|---|---|
| HATCN | N0 : 2.30 Å | N3 : 2.30 Å |
| F4TCNQ | N0 : 2.30 | N4 : 2.30 |
| Cs2CO3 | O2 : 2.40 | O1 : 2.40 |
| Cu4I4 | I7 : 3.10 | I4 : 3.10 |
| LiF32 | F11 : 2.50 | F1 : 2.50 |
| Al4O6 | O4 : 2.20 | O6 : 2.20 |

대칭상 두 끝의 에너지는 **같아야 한다**. 따라서 `E(end) − E(start)`는 그 위에
얹힌 장벽에 대한 **공짜 오차막대**다. 이 값이 E_d에 비해 작지 않으면 두 끝이
같은 전자 상태로 수렴하지 않았다는 뜻이고, 장벽은 그 오차를 그대로 물려받는다.
`123_read_gjf_logs.py`가 E_d 옆에 함께 출력하고 불일치를 표시한다.

(참고: v5 Bphen의 endpoint gap은 −0.512 eV였다. 장벽을 말할 상태가 아니었다.)

#### 경로 길이 산정

`npath`는 이제 흡착원자 시작·끝점 사이 거리가 아니라 **site가 움직인 거리**로
정한다. cage에서는 두 값이 크게 다르다 — Al4O6는 산소 간 2.83 Å를 뛰는데
흡착원자 두 위치는 각자 자기 법선으로 들려 있어 5.7 Å 떨어져 있다. 현(chord)
기준으로 나누면 필요의 두 배로 촘촘히 샘플링해 작업량이 배가 된다.

### v7 최종

246 jobs / 22 folders. 루트는 v6 그대로(PBE0 + XQC,VShift=300).

---

## v8 — 32코어 / 768 GB 워크스테이션에 맞춘 실행 구조 (2026-09-11)

### CPU 30%는 고장이 아니었다

`%NProcShared=16`, 32 코어 + SMT = 논리 프로세서 64개. 상한이 **16/64 = 25%**이고
OS 오버헤드를 더하면 30%다. Gaussian은 받은 스레드를 거의 다 쓰고 있었고,
**기계의 3/4이 놀고 있었을 뿐이다.**

`.out`의 `Job cpu time = Elapsed time`(13분 47.0초 vs 13분 46.3초)은 근거가 못 된다.
G16W는 cpu를 **1초 단위 정수로만** 기록한다 — 파일 안 모든 cpu 값이 정수다.
스레드 수 판단에 쓸 수 없다. (이 함정에 앞서 한 번 빠졌던 적이 있다.)

스레드를 더 줘도 안 빨라진다. 475 basis function SP는 16스레드 한참 전에 스케일이
멈춘다 — Fock 조립은 병렬화되지만 대각화와 DIIS 부기는 안 된다.

### 진짜 병목은 공유 스크래치였다

v7의 `RUN_ALL.bat`은 `GAUSS_SCRDIR=%TEMP%\gauscr` 하나를 썼다. G16W 프로세스
여러 개가 공존할 수 없는 이유가 스레드 수가 아니라 **이것**이었다.

**각 후보 폴더는 이미 자기 `.chk`를 가진 독립 궤도 체인**이므로, 폴더가 병렬화의
자연스러운 단위다. 체인을 쪼갤 필요가 없다. `run_campaign.py`가 (물리코어/8)개
폴더를 동시에 돌리고 **워커마다 별도 스크래치**를 준다. 이 기계에서 4개 동시,
CPU 30% → 90%, 처리량 약 4배.

작업당 `%NProcShared=8`, `%Mem=48GB` (4 × 48 = 192 GB / 768).

### VShift는 필요한 곳에만

`Bphen_t3_z1`이 **이전 점 궤도를 읽고도 38 사이클**을 썼다. v5에서 같은 점이
5 사이클이었다. 레벨시프트는 정의상 업데이트를 damping한다 — Harris guess에는
필요하지만 이미 답 위에 앉아 있을 때는 순수 낭비다.

| | 루트 |
|---|---|
| 폴더 첫 작업 | `SCF=(XQC,MaxCycle=128,VShift=300)` |
| 체인 작업 | `SCF=(XQC,MaxCycle=128)` |

**단, 이건 한 점에서의 추론이지 양쪽을 갈라 잰 측정이 아니다.** v6에서 PBE0와
VShift가 같이 바뀌었다. 체인 작업이 여전히 수십 사이클이면 원인은 시프트가 아니다.

### v7 패키지의 실제 버그: 작업 순서

알파벳 정렬에서 `<name>_t0_z0_ref`는 `<name>_t0_z0` **바로 뒤**에 온다. v7의
`for /r %%F in (*.gjf)` 루프는 기준점 재실행을 **두 번째로** 돌렸을 것이고,
그러면 자기 자신의 궤도만 읽는다 — 벗어나려던 바로 그 guess다.

→ 폴더마다 `ORDER.txt`를 쓰고 러너가 그걸 따른다. 순서를 파일명에서 추론하지 않는다.

또 실패한 작업 뒤로 체인을 계속 가면 이후 점들이 낡은 `.chk`를 읽어 장벽이 서로
다른 전자 상태로 만들어진다. → **폴더는 첫 실패에서 멈추고**, 다른 폴더는 계속.

### in-core 적분은 가정하지 않았다

475 bf의 2전자 적분은 약 51 GB로 메모리에 들어가긴 한다. 다만 PBE0 비용은 교환
적분과 XC 사분법으로 갈리고 전자만 이득을 본다. 러너가 작업별 소요시간을 찍으므로
한 점을 `SCF=(...,InCore)`로 손수 돌려 비교하면 된다. 4배는 작업 병렬화에서
나오고, 이건 그 위 몇 %다. 동시 4작업에서 메모리 실패를 무릅쓸 값어치는 없다.

### 실행

```
RUN_ALL.bat            (또는)  python run_campaign.py
python run_campaign.py --workers 4 --g16 C:\G16W
```

중단 후 재실행하면 끝난 작업은 건너뛴다. 246 jobs / 22 folders.

### v8 실행 확인 및 런처 버그 (2026-09-11)

워크스테이션 실측: **64 logical processors, 4 workers**. SMT 켜진 32코어가 맞았고,
`%NProcShared=16`이 64개 중 25%였다는 진단이 그대로 확인됐다.

**정정**: v8 노트에 "CPU 30% → 90%"라고 썼는데 **50%가 맞다.** 4 워커 × 8스레드 =
32스레드이고 작업 관리자는 논리 64개 기준으로 센다. 물리 코어 32개를 다 쓰는
상태이며 SMT 짝만 비어 있다. `--workers 8`로 SMT까지 채울 수는 있으나 Fock 조립은
부동소수점 유닛을 공유하므로 이득은 10~25% 수준이다.

**런처 버그**: `RUN_ALL.bat`이 `where python`을 먼저 확인했는데, Windows의
`%LOCALAPPDATA%\Microsoft\WindowsApps\python.exe` 스텁이 **존재하고 `where`에
잡히며** 실행하면 "Python" 한 줄만 찍고 끝난다. 더블클릭했으면 아무 일도 일어나지
않았을 것이다. 이 기계가 정확히 그 상태였다 (실제 인터프리터는
`C:\Users\JHKIM\miniforge3\python.exe`).

수정: 알려진 conda/miniforge 경로를 **PATH보다 먼저** 확인하고, WindowsApps 경로는
건너뛰고, 후보마다 `-c "import sys"`를 실제로 실행해 코드를 돌릴 수 있는지 검증한다.
존재 여부만으로는 스텁과 진짜를 구분할 수 없다.

`run_campaign.py`의 docstring에 `C:\G16W`가 들어 있어 SyntaxWarning이 났다 (`\G`).
docstring을 raw string으로 바꿨다. 실행에는 영향 없었다.

---

## v8 중간 점검 — 92개 완료 시점 (2026-09-11)

**92/92 Normal termination.** 그리고 v7/v8에서 넣은 두 장치가 실제로 작동하는 것이
확인됐다.

### 기준점 재실행(`_t0_z0_ref`)이 실제로 한 건 잡았다

| 폴더 | ref − first |
|---|---|
| Al4O6, Cs2CO3, Cu4I4, LiF32, Me3PO, DMABN | **+0.000** (소수점 8자리까지 동일) |
| BTD | +0.000 (4e-9 Ha) |
| **F4TCNQ** | **−0.045 eV** |

7개 폴더에서는 첫 작업이 체인과 같은 해에 앉았다. **F4TCNQ만 첫 작업이 더 높은
해에 앉았고 체인이 더 낮은 해를 찾았다.** 이 장치가 존재하는 이유가 바로 그것이고,
리더는 둘 중 낮은 쪽을 기준점으로 쓴다.

### 체인 작업의 VShift 제거 — 검증됨

v8 노트에 "한 점에서의 추론이지 측정이 아니다"라고 유보를 달았었다. 이제 측정됐다.

| 폴더 | 첫 작업(VShift 있음) | 체인(VShift 없음) |
|---|---|---|
| Al4O6 | 47 cyc | 13–15 |
| Cu4I4 | 40 | 12–15 |
| DMABN | 36 | 14–19 |
| Cs2CO3 | 28 | 13–16 |
| LiF32 | 26 | 11–13 |
| Me3PO | 26 | 12–14 |
| Bphen | 129(DIIS 실패) | **18–20** |

v7에서 `Bphen_t3_z1`이 VShift를 켠 채 **38 사이클**이었다. 지금 같은 급의 체인
작업이 18–20이다. **대략 절반**으로 줄었다. 추론이 맞았다.

### 리더 스크립트의 버그 두 개 (이 데이터가 드러냄)

**버그 1 — S² 정규식이 대부분의 작업에서 아무것도 못 읽고 있었다.**
`S**2 =` 형식은 quadratic convergence 요약에만 찍힌다. DIIS로 수렴한 작업에는
없다. 92개 중 **71개에서 필터가 조용히 무동작**이었다. 모든 UKS 작업에 찍히는
`S**2 before annihilation` 을 읽도록 수정.

**버그 2 — 0.7525 임계값은 PBE에서 보정한 값이라 PBE0에 안 맞는다.**
92개 PBE0 작업의 S² 분포: 최소 0.7513 / 중앙값 0.7518 / 최대 0.8455.
0.7525로 자르면 **21개가 기각**되는데, 그중에는 이웃과 에너지가 mHartree 단위로
일치하는 정상 점들이 들어 있다(예: `Al4O6_t1_z0` S²=0.7529, 15 사이클, 정상).
하이브리드는 전반적으로 오염이 크다.

→ **절대 임계값 대신 폴더 상대값**으로 바꿨다. 폴더 중앙값 + 0.02를 넘는 점만
다른 상태로 보고 기각한다. 절대값은 폴더 전체 경고로만 남긴다(중앙값 > 0.77).

### F4TCNQ는 실제로 스핀 오염됐다 — 한 점이 아니라 폴더 전체

S² 범위 **0.7759–0.8455** (중앙값 0.805). 다른 모든 폴더는 0.7513–0.7608이다.
이건 이상 점 하나가 아니라 **Ag → F4TCNQ 전하이동이 만드는 진짜 open-shell
diradicaloid**다. 단일참조 doublet으로는 잘 기술되지 않는다.

F4TCNQ가 현재 1위(0.305 eV)인데 **그 값은 이 caveat를 달고 봐야 한다.**

### 중간 결과 (9개 폴더, Bphen 진행 중)

| 후보 | E_d (eV) | 끝−시작 (오차막대) | 비고 |
|---|---|---|---|
| F4TCNQ | 0.305 | −0.112 | S² ~ 0.805 전 구간 |
| DMABN | 0.223 | +0.097 | 최소점이 point 1 |
| Me3PO | 0.157 | +0.157 | 최소점이 point 3 |
| BTD | 0.124 | +0.124 | 최소점이 point 1 |
| Cs2CO3 | 0.108 | −0.079 | |
| LiF32 | 0.040 | −0.064 | |
| Al4O6 | 0.000 | −0.074 | 최소점이 point 3 |
| Cu4I4 | 0.000 | −0.140 | 최소점이 point 3 |

### 두 가지 경고를 심각하게 받아들여야 한다

**1. 오차막대가 신호와 맞먹는다.** site2site 경로의 두 끝은 대칭상 같아야 하는데
0.06–0.16 eV 어긋난다. Cs2CO3는 장벽 0.108에 오차 0.079 — 73%다. LiF32는 0.040에
0.064로 **오차가 더 크다**. 원인은 두 가지가 섞여 있다: SCF 해 불일치와,
`outward()`가 1200개 이산 방향 중에서 고르기 때문에 두 끝이 정확한 대칭상이
아니라는 기하 잡음. 후자는 방향을 국소 정련하면 줄일 수 있다.

**2. 절반의 후보에서 경로 최소점이 끝이 아니라 중간에 있다.**
Al4O6는 O 위(t0, t4)보다 **Al 다리 위(t3)를 선호**하고, Cu4I4도 t3(Cu 근처)가
최저다. 즉 입력 구조가 Ag를 놓은 자리가 **결합 site가 아니다**. 이건 실패가
아니라 발견이다 — 다만 장벽은 인접한 두 *극소점* 사이에서 재야 하므로, 이
후보들은 경로 정의를 다시 잡아야 한다. 리더가 이제 이 경우를 표시한다.

---

## v9 — 오차를 장벽보다 작게 만들기 (2026-09-11)

"이 값을 스크리닝 지표로 쓸 수 있게 오차를 줄여라"에 대한 답. 먼저 v8 데이터로
**오차를 원인별로 분해**했다. 결론은 SCF도 기저도 범함수도 아니었다.

### 주범: 흡착원자 높이가 이완되지 않았다

**두 높이로는 최소점을 괄호에 넣을 수 없다.** 둘 중 낮은 쪽은 언제나 스캔의
끝점이므로, 이완된 게 아니라 임의의 두 값 중 하나를 고른 것이다.

| 후보 | \|E(z0) − E(z1)\| 최대 (eV) |
|---|---|
| DMABN t3 | **0.456** |
| Me3PO t4 | **0.424** |
| F4TCNQ | **0.352** |
| Cs2CO3 | 0.186 |
| Al4O6 t3 | 0.176 |

**v8 표의 어떤 장벽보다 크다.** 게다가 경로를 따라 변한다(DMABN t0 0.008 →
t3 0.456). 일정한 offset이면 상쇄되지만, 변하니까 **곡선의 모양 자체를 왜곡**한다.

어느 높이가 이기는지가 경로 중간에 뒤집히는 것이 그 증거다:

```
BTD      z0  z0  z1  z1  z1
Cs2CO3   z0  z0  z0  z1  z0
Me3PO    z0  z0  z0  z1  z1
```

전체 42점 중 z0 26회 / z1 16회. v8 "장벽"의 상당 부분이 *어느 높이가 이겼는가*다.

### 대칭화는 해결책이 아니다 (측정함)

| 후보 | E_d 원본 | 대칭화 | 변화 |
|---|---|---|---|
| Me3PO | 0.157 | 0.000 | −0.157 |
| DMABN | 0.223 | 0.053 | −0.170 |
| F4TCNQ | 0.305 | 0.361 | +0.056 |
| LiF32 | 0.040 | 0.072 | +0.032 |

대칭화가 장벽보다 큰 폭으로 값을 바꾼다. 이건 보정이 아니라 **원본이 물리적으로
매끄러운 곡선이 아니라는 증거**다. 체인의 방향성 drift(반대칭 성분)가 주범이면
대칭화가 들어야 하는데, 안 든다 → 주범은 기하다.

### v9 수정

**1. 높이 3점: dz = −0.20, +0.20, +0.60 Å**
접촉높이가 26/42로 이겼으므로 최소점이 그 양쪽에 걸쳐 있다 → **아래로도 내려가야
한다.** 반발벽 안쪽 점 하나가 있어야 포물선 피팅으로 최소점을 잡을 수 있다.
접촉거리의 85% 안으로 들어가는 높이는 피팅에서 제외한다(벽이 피팅을 끌어당김).

리더가 `relaxed()`로 최저점과 양 이웃에 포물선을 맞춰 **이완된 높이에서의 에너지**를
반환한다. 최저점이 여전히 스캔의 끝이면 원값 + `height not bracketed` 플래그.

비용: 후보당 11 → 16 작업. **358 작업, v8의 약 1.45배.**

**2. 장벽에 자기 오차막대를 붙인다**
동등 site 간 hop은 중점 대칭이므로 E(t) = E(1−t)여야 한다. 최대 불일치의 절반이
**장벽과 같은 방식으로 측정된 오차막대**다. `E_d > 3σ`를 못 넘기면
`barrier below its own error`로 표시. **v8 데이터에서는 8개 중 6개가 탈락한다.**

**3. 공통 기준 분자 대비 상대값도 함께 출력**
스크리닝에 필요한 건 순서지 절대값이고, 절대값이야말로 이 프로토콜이 가장 못 맞추는
부분이다(rigid 기판, 작은 기저, 막이 아닌 단분자 — 모두 **모든 후보에 같은 방향으로**
작용). 기준 대비 차이는 스크리닝이 쓰는 것만 남기고 못 믿을 것을 뺀다.

### 여전히 못 고치는 것

- **기판이 막이 아니라 분자 하나다.** 이웃 분자도 벌크 분극도 없다. 슬랩이면
  숫자가 2배까지 달라질 수 있다.
- **흡착원자가 경로에서 옆으로 못 벗어난다.** 장벽은 여전히 진짜 MEP 값의 상한이다.
- **F4TCNQ는 전 구간 스핀 오염**(S² 0.776–0.846). 1위 후보라 특히 문제다.

→ 그래서 출력은 **장벽 표가 아니라 스크리닝 순서**다.

### 최종적으로 지표를 정당화하는 것

계산만으로는 끝나지 않는다. **6종 closure 두께 실측과의 상관**이 이 지표를
검증하는 유일한 방법이고, 그게 되면 절대값이 거칠어도 "스크리닝 기준을 제시했다"는
주장이 성립한다.

---

## v8 캠페인 완료 — 22/22 폴더, 6시간 (2026-09-12)

업로드된 236개 전부 Normal termination. 예상(6~8시간)과 일치.

**LiF32는 11개 중 1개만 업로드됐다.** 계산은 정상 완료했고(중간 스냅샷에 전부 있음)
분석에는 그걸 합쳐 썼다. 리포지토리 쪽만 다시 올리면 된다.

### HATCN — VShift 판단 최종 검증

| 작업 | 사이클 |
|---|---|
| t0_z0 (첫, VShift) | 24 (+ DIIS 128 실패) |
| t0_z1 | 33 |
| t1~t4 체인 | 23–28 |

다른 폴더 체인이 11–22인 것에 비하면 HATCN은 1.5배 어렵지만 **실패는 없다.**
VShift를 체인에서 뺀 판단은 유지.

### 순위를 못 쓰게 만드는 구조적 문제 두 개

**1. toface 경로는 확산 장벽이 아니다. 22개 중 10개가 여기 해당한다.**

에너지 프로파일(z0, eV):

```
Bphen     0.101  0.189  0.471  0.971  0.790
pyridine  0.010  0.042  0.228  0.661  0.750
```

**끝까지 단조 상승한다.** 이건 hop이 아니라 Ag를 유리한 가장자리 site에서 고리
중심으로 끌어올린 것이고, 값은 **부등가 두 site의 결합에너지 차**다. 장벽이 아니다.

그런데 원본 순위에서 Bphen(0.493)이 1위, pyridine(0.299)이 5위였다. Bphen은
이 프로젝트 자체 예측으로 closure 28 nm — 최악의 후보다. 순위가 뒤집혀 있었다.

더 근본적으로: **결합 site가 한 종류뿐인 분자는 한 분자 위에서 hop을 정의할 수
없다.** 갈 곳이 없다. 이건 경로를 잘 잡아서 될 일이 아니라 **이량체나 슬랩**이
필요한 문제다.

**2. 경로 시작점이 결합 극소점이 아닌 경우가 절반이다.**

HATCN은 극소점이 point 1, Cu4I4·Al4O6는 point 3이다. t=0에서 재면 틀린다.
→ **경로 위 최대 상승폭**(max − min)으로 정의를 바꿨다. 등록(registration)에
의존하지 않는다. t=0 기준값은 입력 구조가 site를 얼마나 빗나갔는지 보여주는
지표로 옆에 남겨둔다.

### 정의를 고친 뒤 site2site 결과

| 후보 | E_d (eV) | ± | t0 기준 | 비고 |
|---|---|---|---|---|
| Mo3O8 | 0.579 | 0.289 | 0.579 | |
| Cu4I4 | 0.471 | 0.225 | 0.000 | site가 point 3 |
| F4TCNQ | 0.418 | 0.110 | 0.305 | S² ~ 0.805 |
| Mo3O9 | 0.356 | 0.135 | 0.270 | site가 point 2 |
| Al4O6 | 0.303 | 0.044 | 0.000 | site가 point 3 |
| **HATCN** | **0.254** | **0.127** | 0.110 | site가 point 1 |
| BTD | 0.190 | 0.080 | 0.124 | |
| triazine | 0.189 | 0.090 | 0.189 | |
| Cs2CO3 | 0.186 | 0.085 | 0.108 | |
| pDCNB | 0.142 | 0.071 | 0.118 | |
| LiF32 | 0.104 | 0.032 | 0.040 | |
| oDCNB | 0.034 | 0.005 | 0.000 | |

**12개 전부 `E_d > 3σ`를 통과하지 못한다.** 높이가 한 점도 괄호에 안 잡혔으므로
당연한 결과다 — 이게 v9가 고치는 것이다.

HATCN이 정의를 고치자 0.110 → **0.254**로 올라가, 폐기된 psi4 값 0.286에 가까워졌다.
다만 ±0.127이라 아직 아무것도 주장할 수 없다.

리더에서 psi4 0.286 대조 문구를 제거했다. 그 값은 실격시킨 DIIS 계열 경로에서
나온 것이라 검증 목표가 될 수 없다.

### 다음

1. **v9** (높이 3점 + 포물선 이완) — 가장 큰 오차원을 제거
2. **toface 10종은 이량체 경로로 재정의** — 한 분자로는 정의 불가
3. 그 다음에야 순위를 말할 수 있다
